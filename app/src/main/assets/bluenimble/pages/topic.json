{
	"layers": {
		"header": [
			"text static.icons.back ? icon", "image static.images.logo ? logo", "text view.topic.title ? title primary rounded"
		],
		"main": [
			"list view.topic.examples ? template=index.menuTpl layout=grid"
		]
	}
}