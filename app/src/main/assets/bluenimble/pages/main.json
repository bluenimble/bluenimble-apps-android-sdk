{
	"layers": {
		"header": [
			"image static.urls.logo ? logo", "text static.title.short ? title"
		],
		"main": [
			"list static.menu ? template=main.menuTpl layout=grid"
		],
		"! menuTpl": [
			"text record.icon ? icon", "/", "text record.title"
		]
	}
}