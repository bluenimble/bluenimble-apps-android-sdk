{
	"layers": {
		"header": [
			"image static.urls.logo ? logo", "text  static.title.short ? title"
		],
		"main": [
			"list static.menu ? template=menuItem"
		],
		"menuItem": [
			"image record.icon ? icon", "/", "text record.title"
		]
	}
}