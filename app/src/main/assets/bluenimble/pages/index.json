{
	"layers": {
		"header": [
			"image static.images.logo ? logo", "text static.title.short ? title primary rounded"
		],
		"main": [
			"list static.menu ? template=menuTpl layout=grid cols=3"
		],
		"! menuTpl": [
			"text record.icon ? rounded icon", "/", "text record.title ? center icon-title"
		]
	}
}